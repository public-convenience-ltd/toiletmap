{
  "name": "toiletmap",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "yarn lint && CI=true lerna run test --parallel",
    "release": "lerna version --conventional-commits --push --exact -m \"build: release %s\"",
    "lint": "eslint . && stylelint \"packages/{ui,admin}/**/*.css\"",
    "prettify": "prettier --write \"./**/*.{js,json}\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neontribe/gbptm.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/neontribe/gbptm/issues"
  },
  "homepage": "https://github.com/neontribe/gbptm#readme",
  "lint-staged": {
    "*.js": [
      "eslint --max-warnings 0",
      "prettier --write"
    ],
    "*.css": "stylelint"
  },
  "devDependencies": {
    "@commitlint/cli": "8.3.5",
    "@commitlint/config-conventional": "8.3.4",
    "husky": "4.2.3",
    "lerna": "3.20.2",
    "lint-staged": "10.1.3",
    "prettier": "2.0.4",
    "stylelint": "13.3.2",
    "stylelint-config-standard": "20.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -e",
      "pre-commit": "lint-staged"
    }
  }
}
