import { Meta } from '@storybook/react';
import LocationSearch from '.';

export default {
  name: 'LocationSearch',
  component: LocationSearch,
} as Meta;

export const Default = (props) => <LocationSearch {...props}></LocationSearch>;
