query minimumViableLooResponse($limit: Int!) {
  loos(filters: {}, pagination: { limit: $limit }) {
    loos {
      id
      name
      location {
        lat
        lng
      }
      noPayment
      allGender
      automatic
      accessible
      babyChange
      radar
      campaignUOL
    }
}
